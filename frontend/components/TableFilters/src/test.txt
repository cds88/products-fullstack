import React, { useState } from "react";
import {
  FormControl,
  InputLabel,
  Select,
  MenuItem,
  Box,
  SelectChangeEvent,
} from "@mui/material";

const BeautifulDropdown = () => {
  const options = [
    { name: "Essence", id: 1 },
    { name: "Velvet Touch", id: 2 },
  ];

  const [selectedId, setSelectedId] = useState("");

  const handleChange = (event: any) => {
    console.log(event.target.value);
  };

  return (
    <Box sx={{ minWidth: 196 }}>
      <FormControl variant="outlined" fullWidth>
        <InputLabel id="select-label">Choose an Option</InputLabel>
        <Select
          labelId="select-label"
          value={selectedId}
          onChange={handleChange}
          label="Choose an Option"
          sx={{
            backgroundColor: "red", // Background color of the Select component
            color: "darkblue", // Font color of the selected value
            "& .MuiSvgIcon-root": {
              color: "#0a0a0a", // Color of the dropdown icon
            },
          }}
        >
          {options.map((option) => (
            <MenuItem
              key={option.id}
              value={option.id}
              sx={{
                backgroundColor: "lightgray", // Background color of MenuItem
                color: "black", // Font color of MenuItem
                "&:hover": {
                  backgroundColor: "gray", // Background color on hover
                },
                "&.Mui-selected": {
                  backgroundColor: "darkgray", // Background color when selected
                  color: "white", // Font color when selected
                  "&:hover": {
                    backgroundColor: "gray", // Background color when selected and hovered
                  },
                },
              }}
            >
              {option.name}
            </MenuItem>
          ))}
        </Select>
      </FormControl>
    </Box>
  );
};

export default BeautifulDropdown;
